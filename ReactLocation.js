/**
 * @providesModule AndroidLocation
 */

'use strict';

/**
 * This exposes the native AndroidLocation module as a JS module
 */
var { NativeModules } = require('react-native');
module.exports = NativeModules.AndroidLocation;
